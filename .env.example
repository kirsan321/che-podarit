# Copy to .env.local and fill in. Never commit .env.local.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only (route handlers). Never expose to the client.
SUPABASE_SERVICE_ROLE_KEY=
ANTHROPIC_API_KEY=
